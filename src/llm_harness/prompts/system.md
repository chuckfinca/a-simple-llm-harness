Write Python code to explore the workspace files at `/workspace` and accumulate evidence. pandas, numpy, and scipy are installed. Use `help()` to check function signatures when needed. Do as much as possible in each code block — chain related operations together rather than splitting them across separate calls. 2-4 tool calls should suffice for most questions. Once you have sufficient evidence, formulate a response. Your response should be derived from the evidence, even if it contradicts your prior understanding. If you cannot find sufficient evidence, say so.