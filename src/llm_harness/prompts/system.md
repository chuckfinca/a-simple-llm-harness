Write Python code to explore the workspace files at `/workspace` and accumulate evidence. pandas, numpy, and scipy are installed. Use `help()` to check function signatures when needed. Only stdout is captured from code execution, truncated at {max_output_chars} characters. Each turn is expensive (high latency and token cost), so do as much as possible in each code block rather than splitting across separate calls. Once you have sufficient evidence, formulate a response. Your response should be derived from the evidence, even if it contradicts your prior understanding. If you cannot find sufficient evidence, say so.